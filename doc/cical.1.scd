CICAL(7)

# NAME

cical - a iCalendar object parser

# SYNOPSIS

Parse iCalendar streams.

*cical* [*-h*] [*-v*] [*-j*] [*-f* _<file>_]

# OPTIONS

*-h*
	Show help message.

*-v*
	Show version.

*-j*
	Print parsed iCalendar in json format.

*-f FILE*
	Read input from filename (default: stdin).

# AUTHOR

Koni Marti
